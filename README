Jumpstartr is meant to be a minimal library to pull down when you need to start codeing your next *small* project. It's not a full feature framework, nor will it ever be.

It's configured to use less.js with is a dynamic CSS processor so you can use variables, mixins, etc.